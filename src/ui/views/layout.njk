<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="PollyPi">
    <meta name="author" content="Nicolas Inden">
    {% block head %}{% endblock %}
    <link rel="stylesheet" href="/css/app.css" />
    <script defer src="/js/app.js"></script>
    {% block extrajs %}{% endblock %}
</head>
<body>

    <header>
      <div class="navbar navbar-light bg-light shadow-sm">
        <div class="container d-flex justify-content-between">
          {% if title is defined %}
          <a href="{{ title.href }}" class="navbar-brand d-flex align-items-center">
            <i class="fas fa-poll"></i>
            <strong>{{ title.name }}</strong>
          {% else %}
          <a href="/" class="navbar-brand d-flex align-items-center">
            <i class="fas fa-poll"></i>
            <strong class="ms-2">PollyPi</strong>
          {% endif %}
          </a>
          {% if poll is defined %}
          <div class="lead">
            Umfragecode: {{ poll.code }}
          </div>
          {% endif %}
        </div>
      </div>
    </header>

    <main role="main" class="container pt-3">
        {% block content %}{% endblock %}
    </main>

    <footer class="container text-muted">
      <div class="row">
        <p class="col">PollyPi &copy; {{ currentYear }} Nicolas Inden</p>
        <p class="col"><a class="float-end" href="https://github.com/smashnet/PollyPi">Visit PollyPi on GitHub</a></p>
      </div>
    </footer>
</body>
</html>